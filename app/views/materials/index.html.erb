<div class="max-w-5xl mx-auto">
  <div class="flex items-center justify-between mb-6">
    <div>
      <h1 class="text-2xl font-bold text-slate-900"><%= t(".title") %></h1>
      <p class="text-sm text-slate-500 mt-1"><%= t(".subtitle") %></p>
    </div>
    <%= link_to t(".new_button"), new_material_path,
        class: "inline-flex items-center gap-2 bg-amber-600 hover:bg-amber-700 text-white px-4 py-2 rounded-lg text-sm font-semibold transition-colors" %>
  </div>

  <% if @materials.any? %>
    <div class="bg-white rounded-xl border border-slate-200 shadow-sm overflow-hidden">
      <table class="w-full text-sm">
        <thead class="bg-slate-50 border-b border-slate-200">
          <tr>
            <th class="px-4 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t(".col_name") %></th>
            <th class="px-4 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t(".col_description") %></th>
            <th class="px-4 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t(".col_category") %></th>
            <th class="px-4 py-3 text-left text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t(".col_unit") %></th>
            <th class="px-4 py-3 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t(".col_default_price") %></th>
            <th class="px-4 py-3 text-right text-xs font-semibold text-slate-500 uppercase tracking-wider"><%= t(".col_actions") %></th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-100">
          <% @materials.each do |material| %>
            <tr class="hover:bg-slate-50">
              <td class="px-4 py-3 font-medium text-slate-900"><%= material.name %></td>
              <td class="px-4 py-3 text-slate-600"><%= material.description.presence || t("common.not_provided") %></td>
              <td class="px-4 py-3 text-slate-600"><%= material.category.humanize.downcase.split.map(&:capitalize).join(" ") %></td>
              <td class="px-4 py-3 text-slate-600"><%= material.unit.presence || t("common.not_provided") %></td>
              <td class="px-4 py-3 text-right font-mono text-slate-900">$<%= number_with_precision(material.default_price, precision: 4) %></td>
              <td class="px-4 py-3 text-right">
                <div class="flex items-center justify-end gap-2">
                  <%= link_to t("common.edit"), edit_material_path(material),
                      class: "text-xs text-slate-600 hover:text-slate-900 font-medium" %>
                  <%= button_to t(".archive_button"), material_path(material), method: :delete,
                      class: "text-xs text-red-600 hover:text-red-800 font-medium" %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-12 text-center">
      <p class="text-slate-700 font-medium"><%= t(".empty_title") %></p>
      <p class="text-slate-500 text-sm mt-1"><%= t(".empty_description") %></p>
    </div>
  <% end %>
</div>
