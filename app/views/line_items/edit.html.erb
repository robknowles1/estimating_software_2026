<div class="mb-6">
  <h1 class="text-2xl font-bold text-slate-900"><%= t(".title") %></h1>
  <p class="text-slate-500 text-sm mt-0.5"><%= t(".subtitle", section: @section.name, estimate: @estimate.estimate_number) %></p>
</div>

<div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 max-w-3xl">
  <%= render "form", line_item: @line_item, section: @section, estimate: @estimate %>
</div>
