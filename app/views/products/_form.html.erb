<%# Shared field style helpers %>
<% input_cls = "w-full border border-slate-300 rounded-lg px-3 py-2 text-sm text-slate-900 focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition-colors" %>
<% label_cls = "block text-xs font-medium text-slate-700 mb-1" %>

<%# Validation errors %>
<% if f.object.errors.any? %>
  <div class="bg-red-50 border border-red-300 rounded-lg p-4 mb-6">
    <p class="text-sm font-semibold text-red-700 mb-2">
      <%= t("products.form.errors.other", count: f.object.errors.count) %>
    </p>
    <ul class="list-disc list-inside space-y-1">
      <% f.object.errors.each do |error| %>
        <li class="text-sm text-red-600"><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%# ── Identity ── %>
<div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 mb-6">
  <h2 class="text-sm font-semibold text-slate-700 mb-4"><%= t("products.form.basic_heading") %></h2>
  <div class="grid grid-cols-3 gap-4">
    <div class="col-span-2">
      <%= f.label :name, class: label_cls %>
      <%= f.text_field :name, class: input_cls, placeholder: "e.g. MDF Base 2-door" %>
    </div>
    <div>
      <%= f.label :unit, class: label_cls %>
      <%= f.text_field :unit, class: input_cls, placeholder: "EA" %>
    </div>
    <div class="col-span-3">
      <%= f.label :category, class: label_cls %>
      <%= f.text_field :category, class: input_cls, placeholder: "e.g. Base Cabinets" %>
    </div>
  </div>
</div>

<%# ── Materials ── %>
<div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 mb-6">
  <h2 class="text-sm font-semibold text-slate-700 mb-4"><%= t("products.form.materials_heading") %></h2>

  <%# Column headers %>
  <div class="grid grid-cols-12 gap-2 mb-2 px-1">
    <div class="col-span-1"></div>
    <div class="col-span-5 text-xs font-semibold text-slate-500 uppercase tracking-wider">Description</div>
    <div class="col-span-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Unit Price</div>
    <div class="col-span-3 text-xs font-semibold text-slate-500 uppercase tracking-wider">Qty</div>
  </div>

  <% [
    { key: :exterior,  label: "Exterior" },
    { key: :interior,  label: "Interior" },
    { key: :interior2, label: "Interior 2" },
    { key: :back,      label: "Back" },
    { key: :drawers,   label: "Drawers" },
    { key: :pulls,     label: "Pulls" },
    { key: :hinges,    label: "Hinges" },
    { key: :slides,    label: "Slides" },
    { key: :locks,     label: "Locks" }
  ].each do |slot| %>
    <div class="grid grid-cols-12 gap-2 mb-2 items-center">
      <div class="col-span-1 text-xs text-slate-500 font-medium"><%= slot[:label] %></div>
      <div class="col-span-5">
        <%= f.text_field :"#{slot[:key]}_description", class: input_cls, placeholder: "e.g. MDF" %>
      </div>
      <div class="col-span-3">
        <%= f.number_field :"#{slot[:key]}_unit_price", class: input_cls, step: "0.0001", min: 0, placeholder: "0.0000" %>
      </div>
      <div class="col-span-3">
        <%= f.number_field :"#{slot[:key]}_qty", class: input_cls, step: "0.0001", min: 0, placeholder: "0.0000" %>
      </div>
    </div>
  <% end %>

  <%# Banding — no qty %>
  <div class="grid grid-cols-12 gap-2 mb-2 items-center">
    <div class="col-span-1 text-xs text-slate-500 font-medium">Banding</div>
    <div class="col-span-5">
      <%= f.text_field :banding_description, class: input_cls, placeholder: "e.g. Melamine edge band" %>
    </div>
    <div class="col-span-3">
      <%= f.number_field :banding_unit_price, class: input_cls, step: "0.0001", min: 0, placeholder: "0.0000" %>
    </div>
    <div class="col-span-3">
      <span class="text-xs text-slate-400 italic">Flat rate — no qty</span>
    </div>
  </div>

  <%# Other material cost %>
  <div class="mt-4 pt-4 border-t border-slate-100">
    <div class="grid grid-cols-12 gap-2 items-center">
      <div class="col-span-4 text-xs font-medium text-slate-700"><%= t("products.form.other_material_cost") %></div>
      <div class="col-span-3">
        <%= f.number_field :other_material_cost, class: input_cls, step: "0.01", min: 0, placeholder: "0.00" %>
      </div>
    </div>
  </div>
</div>

<%# ── Labor ── %>
<div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 mb-6">
  <h2 class="text-sm font-semibold text-slate-700 mb-4"><%= t("products.form.labor_heading") %></h2>
  <div class="grid grid-cols-3 gap-4">
    <% %w[detail mill assembly customs finish install].each do |cat| %>
      <div>
        <label class="<%= label_cls %>"><%= cat.capitalize %> Hrs</label>
        <%= f.number_field :"#{cat}_hrs", class: input_cls, step: "0.0001", min: 0, placeholder: "0.0000" %>
      </div>
    <% end %>
  </div>
</div>

<%# ── Equipment ── %>
<div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6 mb-6">
  <h2 class="text-sm font-semibold text-slate-700 mb-4"><%= t("products.form.equipment_heading") %></h2>
  <div class="grid grid-cols-2 gap-4">
    <div>
      <label class="<%= label_cls %>">Equipment Hours</label>
      <%= f.number_field :equipment_hrs, class: input_cls, step: "0.0001", min: 0, placeholder: "0.0000" %>
    </div>
    <div>
      <label class="<%= label_cls %>">Equipment Rate ($/hr)</label>
      <%= f.number_field :equipment_rate, class: input_cls, step: "0.01", min: 0, placeholder: "0.00" %>
    </div>
  </div>
</div>

<%# ── Actions ── %>
<div class="flex items-center gap-3">
  <%= f.submit class: "bg-amber-600 hover:bg-amber-700 text-white px-5 py-2 rounded-lg text-sm font-semibold transition-colors cursor-pointer" %>
  <%= link_to t("products.form.cancel"), products_path,
      class: "text-sm text-slate-600 hover:text-slate-900 font-medium" %>
</div>
