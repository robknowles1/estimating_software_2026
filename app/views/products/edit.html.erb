<div class="mb-6">
  <h1 class="text-2xl font-bold text-slate-900"><%= t(".title") %></h1>
  <p class="text-slate-500 text-sm mt-0.5"><%= t(".subtitle", name: @product.name) %></p>
</div>

<%= form_with model: @product do |f| %>
  <%= render "form", f: f %>
<% end %>
